# 安全政策（Security Policy）

## 支持的版本

| 版本 | 支持状态 |
|------|----------|
| 2.0.0-beta.15+ | ✅ 支持 |
| 2.0.0-beta.14 | ⚠️ 仅安全修复 |
| < 2.0.0-beta.14 | ❌ 不再支持 |

## 报告漏洞

如果发现安全漏洞（数据泄露、远程代码执行、权限绕过等），**请勿公开提交 Issue**。

1. 直接私信仓库维护者（GitHub: mo9652962-ai）
2. 或发送邮件至仓库主页显示的联系邮箱
3. 提供：漏洞描述 / 复现步骤 / 影响范围 / 建议修复

我们承诺：
- 收到报告后 **7 天内**确认并回复
- 修复前**不公开**漏洞细节
- 修复后发布补丁版本并致谢报告者

## 安全设计

本项目遵循 local-first 安全设计：

- **数据本地存储**：题库/做题记录/单词本全部存于本机 SQLite——不上传
- **API Key 加密**：Windows 使用 DPAPI、Android 使用 Keystore AES-GCM——加密后落盘
- **依赖安全**：通过 Dependabot 自动跟踪依赖更新（见 `.github/dependabot.yml`）
- **最小权限**：程序仅请求运行所需权限，不采集遥测/统计信息

## 已知安全边界

- 导入的题库文件（Word/PDF/ESQ）在用户本机解析——请只导入可信来源的题库
- AI 模型调用：仅在你主动调用对应功能时，相关内容才会发送到你配置的模型服务商
